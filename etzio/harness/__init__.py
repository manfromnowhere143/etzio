"""Measurement harnesses. The roadmap's rule: no slice ships without an FPR number.

Import the harness directly to avoid re-import side effects when run as a module:

    from etzio.harness.fpr import evaluate, Metrics
"""
